%h1 All Wikis

-if current_user
  .pull-right.col-md-4
    = link_to "New Wiki", new_wiki_path, class: 'btn btn-success'

- @wiki.each do |wiki|
  .media
    .media-body
      %h4.media-heading
        = link_to wiki.title, wiki
        -if wiki.private
          %button.btn.btn-xs.btn-warning{:type => "button"} Private
