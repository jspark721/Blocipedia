<div class="row">
  <div class="col-md-8">
    <div class="media">
      <div class="media-body">
        <h2 class="media-heading"><%= @user.name %></h2>
        <small>
          <%= pluralize(@user.wikis.count, 'wikis') %>
        </small>
      </div>
    </div>
  </div>
</div>

<h2>Wikis</h2>
<%= render @user.wikis %>
